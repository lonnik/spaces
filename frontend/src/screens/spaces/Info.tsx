import { FC } from "react";
import { View } from "react-native";
import { Text } from "../../components/Text";

export const SpaceInfoScreen: FC = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text>Info</Text>
    </View>
  );
};
