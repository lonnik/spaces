apiVersion: v1
kind: Service
metadata:
  name: {{ include "server.name" . }}
  labels: 
    {{- include "server.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  selector:
    app: spaces
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: {{ .Values.service.internalPort }}